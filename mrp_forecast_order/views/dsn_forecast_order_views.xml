<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="dsn_view_forecast_order_tree" model="ir.ui.view">
        <field name="name">dsn.view.forecast.order.tree</field>
        <field name="model">dsn.forecast.order</field>
        <field name="arch" type="xml">
            <tree
                decoration-info="state == 'draft'"
                decoration-warning="state == 'confirm'"
                decoration-success="state == 'done'"
                name="Forecast Order">
                <header>
                    <button name="button_generate_pricelist" type="object" string="Generate Pricelist"/>
                </header>
                <field name="name" />
                <field name="create_date"/>
                <field name="state" widget="badge"
                    decoration-info="state == 'draft'"
                    decoration-warning="state == 'confirm'"
                    decoration-success="state == 'done'"/>
            </tree>
        </field>
    </record>

    <record id="dsn_view_forecast_order_form" model="ir.ui.view">
        <field name="name">dsn.view.forecast.order.form</field>
        <field name="model">dsn.forecast.order</field>
        <field name="arch" type="xml">
            <form string="Forecast Order">
                <header>
                    <button name="button_to_confirm" states="draft" string="Confirm" type="object" class="oe_highlight"/>
                    <button name="reset_to_draft" states="confirm" string="Set to Draft" type="object"/>
                    <field name="state" widget="statusbar"></field>
                </header>
                <sheet>
                    <div class="oe_edit_only">
                        <label for="name" class="oe_inline" string="Document Number"/>
                    </div>
                    <h1>
                        <field name="name" class="oe_inline" readonly="1"/>
                    </h1>
                    <group>
                        <group>
                            <field name="type" invisible="1"></field>
                            <field name="create_date"/>
                        </group>
                        <group>
                             <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}"/>
                        </group>
                        <group>
                        </group>
                    </group>
                    <notebook>
                        <page string="Forecast Detail">
                            <field name="forecast_line_ids" attrs="{'readonly': [('state', '!=', 'draft')]}">
                                <tree editable="bottom">
                                    <field name="product_id" options="{'no_create': True}" required="1" domain="[('sale_ok', '=', True),('detailed_type', '=', 'product')]"/>
                                    <field name="uom_id" readonly="1" force_save="1"></field>
                                    <field name="partner_id" options="{'no_create': True}"/>
                                    <field name="currency_id" invisible="1" force_save="1"></field>
                                    <field name="price" widget="monetary" options="{'currency_field': 'currency_id'}" force_save="1"></field>
                                    <field name="quantity"></field>
                                    <field name="total" widget="monetary" options="{'currency_field': 'currency_id'}"></field>
                                    <field name="forecast_date" required="1"></field>
                                    <field name="is_demand_order" readonly="1" optional="show"></field>
                                    <field name="company_id" groups="base.group_multi_company" options="{'no_create': True}" optional="show"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                </sheet>
                <div class="oe_chatter">
                    <field name="message_follower_ids" widget="mail_followers" />
                    <field name="activity_ids" widget="mail_activity" />
                    <field name="message_ids" widget="mail_thread" />
                </div>
            </form>
        </field>
    </record>    

    <record model="ir.actions.act_window" id="dsn_action_sale_forecast_order">
        <field name="name">Forecast Order</field>
        <field name="type">ir.actions.act_window</field>
        <field name="res_model">dsn.forecast.order</field>
        <field name="domain">[('type', '=', 'forecast_order')]</field>
        <field name="context">{'default_type': 'forecast_order'}</field>
        <field name="view_mode">tree,form</field>
        <field name="help" type="html">
            <p class="oe_view_nocontent_create">
                Click to start a new forecast order process.
            </p>
            <p>
                Forecast orders plan the production of finished goods.
            </p>
        </field>
    </record>
</odoo>